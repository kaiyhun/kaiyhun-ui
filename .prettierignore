dist/
node_modules/
package-lock.json
src/assets/
# Raw post drafts at the repo root (converted into src/content/posts/)
blog_*.md

# Docs live in the project WIKI, not the repo — never format-checked
# here, so CI passes whether or not a docs/ folder exists locally.
docs/

# Playwright MCP scratch output
.playwright-mcp/
