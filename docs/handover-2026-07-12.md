# Session handover — 2026-07-09 → 2026-07-12 (Fable)

The onboarding map for whoever works on this next. Read CLAUDE.md
first (always), then this. Everything below shipped verified against
the production build (`npm run check` green throughout).

## ⚠ Read these two first

1. **`docs/` is currently GIT-IGNORED** (`.gitignore` line `docs/`,
   added by the user mid-session, still uncommitted). Docs committed
   BEFORE the ignore still update normally (architecture, presets,
   homepage-brief, implementation-plan, …) but these exist ONLY on
   this machine: `content-draft.md`, `game.md`, `settings.md`,
   `audit-2026-07-10.md`, `build-flake-oneshot.md`, `about.md`,
   `preset.md`, `tutorial.md`, this file. Possibly deliberate (public
   repo, personal context in docs) — ASK THE USER before changing;
   if it stays, these files need a backup story.
2. **Two PROTECTED easter eggs are Fable's signature** (user gift,
   explicitly to be preserved): the BinaryScene cat/dandelion
   interactivity (rule in CLAUDE.md + memory) and the hero "dreaming"
   idle effect. Never remove; tune only on explicit request.

## What shipped this session (newest first)

| Feature | Where | Doc |
| --- | --- | --- |
| Hero idle "dreaming" (aurora / Matrix glitch after 90s idle, snaps back on input, reduced-motion skipped) | `features/home/hero.tsx` (`HeroDream`), `lib/use-idle.ts` | hero comments + content-draft §25 |
| Game arcade control deck (lit keycaps, playable on touch) | `features/game/control-deck.tsx` | game.md |
| BinaryScene egg: pet the cat / blow dandelions (PROTECTED) | `features/home/binary-scene.tsx` | docstring + content-draft §24 |
| /run gravity-flip runner (hidden: URL + zsh `run` + console hint; noindex, unsitemapped) | `features/game/runner{-core,}.tsx`, `pages/run.tsx` | game.md (design source of truth) |
| /settings theme deck (fanned cards, center card applies instantly, localStorage) + generalized theme engine | `pages/settings.tsx`, `features/settings/theme-deck.tsx`, `lib/theme.ts` | settings.md |
| Theme scope classes (`:root, .theme-default` selector trick) | `index.css` | settings.md |
| Hero printed-cover chrome (film grain, spine rail; user later disabled barcode + ink-misregistration by commenting out) + content-driven `HERO_CTA` | `features/home/hero.tsx`, `content/site.ts`, `index.css` utilities | content-draft §19 |
| Hero CTA disappear fix (`HeroEnter`: mount-driven, never `whileInView` in the hero) | `features/home/hero.tsx` | docstring |
| Homepage audit: pager hygiene (anchor re-sync for keys/scrollbar, ctrl+wheel zoom passthrough, deltaX guard, deltaMode normalization) | `features/home/use-section-pager.ts` | docstring "Wheel-event hygiene" |
| Mobile scroll: snap `mandatory` → `proximity` (free scroll + magnet) | `index.css` | homepage-brief |
| /preset/v2 (WIP page, unlinked): layer-stack mixer (editing-panel UI, per-filter opacity over cumulative combo exports) + shared `PresetTimeline` extraction | `pages/preset-v2.tsx`, `features/presets/preset-{mixer,timeline}.tsx`, `content/presets.ts` | presets.md + content-draft §21 |
| Slider + Switch primitives (shadcn registry, restyled) | `components/ui/{slider,switch}.tsx` | docstrings |
| Header gear + footer Settings link; `run` in zsh + `help`; console P.P.S. hint | header/footer/terminal-prompt/main.tsx | settings.md / game.md |

## User edits to respect (do not "fix back")

- Hero: eyebrow `v0.0.0 — Portfolio`, wordmark `KAIYHUN` uppercase +
  `font-medium` (not the token's 700), CTA label "See my art",
  barcode/misregistration/arrow-icon COMMENTED OUT (kept as toggles;
  `.barcode` + `.text-misregister` utilities in index.css are
  currently unused on purpose). `HERO_COVER.issue` ("v0.0.0") is only
  referenced by the commented barcode block — the eyebrow hard-codes
  it; re-sync if the barcode returns.
- Docstring credit "Fable's" in binary-scene.tsx (user's edit).

## Known issues / watch list

- **Build flake** (rolldown "oneshot canceled"): WORSENING — needed
  4–6 retries this session vs 2–3 before. Mitigation everywhere:
  `for i in 1..6; UV_THREADPOOL_SIZE=16 npm run build && break`.
  Decisive experiment ready in build-flake-oneshot.md
  (`SKIP_BUILD_ENRICHMENT=1`).
- Playwright's fake clock (`page.clock`) does NOT drive Motion
  animations (WAAPI/perf-now mismatch) — mount/unmount asserts work,
  visual fades need real time (this session temporarily lowered
  `DREAM_AFTER_MS` to test; pattern documented here so you don't
  rediscover it).
- chrome-devtools MCP was disconnected all session — playwright MCP
  (`browser_run_code_unsafe`) is the verification workhorse; preview
  at `localhost:4173/kaiyhun-ui/`, kill with `pkill -f vite`.
- Real-device checks still pending (user-owned): proximity snap feel,
  Magic Mouse pager feel, dvh on actual iOS.

## Pending user actions

- Decide the `docs/` gitignore question (above) + commit `.gitignore`.
- Review content-draft §17–25 (all drafts flagged FOR USER REVIEW).
- Untracked at session end: `blog_1.md` (root — draft post material?)
  and `src/content/posts/why-i-started-coding.mdx` (real post, works,
  will ship whenever committed).
- /preset/v2: swap 16 stand-in mixer images + 8 preset sections when
  real V2 exports exist (presets.md documents the swap path).
- Delete placeholder posts + placeholder before/after pairs before
  launch (audit-m11.md checklist).

## Verification norms recap (they saved us repeatedly)

Production build only; hit-tested clicks (never element.click());
network-tier checks for images; keyboard walks; reduced-motion
context; mobile via `newContext({ viewport, hasTouch, isMobile })`;
synthesized momentum via CDP `Input.synthesizeScrollGesture` for
scroll-snap work. Zero lint warnings is the baseline.
