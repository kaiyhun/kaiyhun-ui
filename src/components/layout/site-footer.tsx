/** SiteFooter — minimal site-wide footer. */
export function SiteFooter() {
  return (
    <footer className="border-t border-border px-6 py-12 text-center text-sm text-muted-foreground">
      kaiyhun — bold, motion-heavy portfolio
    </footer>
  )
}
